regex_rules:
  phone:
    pattern: '1[3-9]\d{9}'
    replacement: 'PHONE_MASK'
  email:
    pattern: '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}'
    replacement: 'EMAIL_MASK'
  id_card:
    pattern: '(\d{6})\d{8}(\d{3}[0-9Xx])'
    replacement: 'ID_CARD_MASK'
  bank_card:
    pattern: '\b\d{12,19}\b'
    replacement: 'BANK_CARD_MASK'
dictionary_files:
  - sensitive_names.txt
